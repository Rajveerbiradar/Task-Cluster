Icon-only button with a ≥44px hit target — use for toolbar/menu affordances; blue only when `active`.

```jsx
<IconButton label="Edit" icon={<Icon name="pencil" />} active />
```

Requires `label` (a11y). size default 44 — never go below 44.