Small count/label pill — use for import-preview counts and the like.

```jsx
<Badge tone="accent">3 sections</Badge>
<Badge>12 tasks</Badge>
```

tone: neutral (sunken paper) | accent (blue tint, sparingly).