The signature checkbox — hairline square that becomes a blue fill + white check; use for every task and toggle.

```jsx
<Checkbox checked={done} onChange={setDone} />
```

Checking a task greys its row in place (see TaskRow) — it never reorders. size default 22.