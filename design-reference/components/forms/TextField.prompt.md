Paper text input — use for titles and descriptions; focus deepens the hairline to ink (never a blue glow).

```jsx
<TextField label="Title" value={v} onChange={setV} />
<TextField label="Description" multiline rows={2} />
```

multiline renders a textarea; label sits above in meta type.