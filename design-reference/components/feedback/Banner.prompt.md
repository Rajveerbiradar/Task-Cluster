Quiet date-mode notice — use atop past/future days; states the mode, never alarms.

```jsx
<Banner variant="read-only" />   {/* viewing — read only */}
<Banner variant="planning" />    {/* planning — hidden until then */}
```

variant: read-only | planning | info. Override copy via children.