The floating three-mode pill (check · search · add) — the app's only persistent chrome; active mode is blue.

```jsx
<BottomBar mode={mode} onModeChange={setMode} />
```

mode: check | search | edit | add. The only persistently elevated element — give it room and a shadow.